import { SqliteWorkerClient } from "../SqliteWorkerClient/main";

/**
 * instance we use in GUI
 * you can do whatever
 * every instance have own worker..
 */

export const sqliteWorkerClientInstance01 = new SqliteWorkerClient();
export const sqliteWorkerClientInstance02 = new SqliteWorkerClient();
